# Polici Language

VS Code language support for Polici policy files, including syntax highlighting,
snippets, diagnostics, completion, hover, signatures, and semantic tokens.

The extension starts `polici lsp --stdio`. Configure `polici.binary` when the
executable is not available on `PATH`.
